JECS Luau test template

This is a minimal test template for the JECS Luau project template.

Quickstart:

- cd into the template directory
- run `direnv allow` or just `direnv reload`
- use `devenv run src/main.lua` to run the sample script
